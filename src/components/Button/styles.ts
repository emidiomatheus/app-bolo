import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    button: {
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        backgroundColor: '#E6790B',
        width: '100%',
        height: 50,
        borderRadius: 4,
        marginTop: 12
    },
    textButton: {
      color: '#fff',
      fontWeight: '600',
    }
})